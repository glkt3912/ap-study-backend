# Database Configuration
DATABASE_URL="file:./dev.db"
# Production example:
# DATABASE_URL="postgresql://username:password@localhost:5432/ap_study_db?schema=public"

# Server Configuration (デフォルト値あり)
PORT=8000                    # デフォルト: 8000 (範囲: 1-65535)
NODE_ENV=development         # デフォルト: development

# CORS Configuration (カンマ区切りで複数指定可能、スペースは自動で削除)
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001"  # デフォルト: http://localhost:3000,http://localhost:3001
# 本番環境例: ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com"
# 注意: 開発環境(NODE_ENV=development)では、オリジンなしのリクエスト(curl等)は自動で許可されます

# Logging Configuration
LOG_LEVEL=info               # デフォルト: info (debug, info, warn, error)

# 本番環境の例:
# PORT=3001
# NODE_ENV=production
# DATABASE_URL="postgresql://user:pass@host:5432/dbname"
# ALLOWED_ORIGINS="https://your-frontend.com"